\subsection{Tabulation}

Tabulación es un metodo Bottom Up, en el cual primero se calculan las instancias mas pequeñas del problema.
Se utiliza una tabla dp que contiene las primeras soluciones para caso base, para luego ir rellenando con formula recursiva. La llamada recursiva es en la tabla, 
no en la función.
